# Analytics

Purpose: deeper charts and summary views that sit beside, not inside, the
primary tab shell.

Main components:
- `components/AnalyticsScreen.tsx`

Integration points:
- Reuses chart components from the insights feature
- Reads tasks, finance, budgets, and learning session data through feature hooks
