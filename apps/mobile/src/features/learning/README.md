# Learning

Purpose: track learning sessions and expose learning analytics to the Insights experience.

Main components:
- `learning.hooks.ts`
- `learning.service.ts`

Dependencies and integration points:
- Consumed mainly by `features/insights`
- Stores session data through the shared backend layer
