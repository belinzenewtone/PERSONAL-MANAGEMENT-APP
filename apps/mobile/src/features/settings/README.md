# Settings Feature

Owns personal preferences for the mobile workspace, including biometric relock,
notifications, assistant behavior, and system notification permission access.
UI lives in `components/`, while persisted preference state lives in the
shared store layer.
