# Assistant

Purpose: conversational guidance layer that interprets task, finance, and learning insight data into next actions.

Main components:
- `components/AssistantScreen.tsx`

Dependencies and integration points:
- Uses `features/insights/insights.hooks.ts` for AI insight context
- Intended as the primary AI-facing tab in the revamped product shell
