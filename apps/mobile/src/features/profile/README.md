# Profile

Purpose: account details, password management, and sign-out controls.

Main components:
- `components/ProfileScreen.tsx`

Dependencies and integration points:
- Uses `features/auth/auth.service.ts` for sign-out and password updates
- Reads current user data from `src/store/auth.store.ts`
